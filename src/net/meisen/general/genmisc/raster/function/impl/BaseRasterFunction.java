package net.meisen.general.genmisc.raster.function.impl;

import net.meisen.general.genmisc.raster.definition.impl.BaseRaster;
import net.meisen.general.genmisc.raster.function.IRasterFunction;

/**
 * A <code>Function</code> is used to add additional fields to a
 * {@link BaseRaster} of type <code>T</code>. The result of a
 * <code>Function</code> is based on the interval of a data row.
 * 
 * @author pmeisen
 */
public abstract class BaseRasterFunction implements IRasterFunction {
	// nothing so far
}
